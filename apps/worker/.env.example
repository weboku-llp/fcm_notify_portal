# =============================================================================
# apps/worker — Railway service name: worker
# =============================================================================

SERVICE=worker

# Same Supabase Session pooler URL as api (NOT db.*.supabase.co)
DATABASE_URL=
REDIS_URL=${{Redis.REDIS_URL}}
PORTAL_ENCRYPTION_KEY=
FCM_DRIVER=firebase
LOG_LEVEL=info
