{
  "name": "@notif/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/__tests__/templates.test.ts src/__tests__/fcm-errors.test.ts src/__tests__/device-registration.smoke.test.ts",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@notif/config": "workspace:*",
    "@notif/contracts": "workspace:*",
    "@notif/crypto": "workspace:*",
    "@notif/db": "workspace:*",
    "@notif/logger": "workspace:*",
    "firebase-admin": "^13.0.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
